{
  "name": "v-quicksell",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute quicksell --remote --file=schema.sql",
    "vapid": "node scripts/vapid.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.135.0"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.0.2"
  }
}
